{
  "name":  "dc_compute",
  "title": "Config Test - DataCenter Compute Only",

  "datacenter": {

    "provider": { "vendor": "mock" },

    "compute": {

      "clusters": [
        {
          "cluster": "clusterA",
          "pods": [
            {
              "pod":             "podA",
              "servertype":      "base",
              "version":         1,
              "subnet_group":    "sub1",
              "security_groups": [ "default" ],
              "volumes": [
                { "device": "/dev/sda1", "type": "standard", "size": 8, "boot": true }
              ],
              "count": 3
            }
          ]
        }
      ]
    }

  }

}
